{
  "name": "aldus",
  "version": "0.2.0-next.33",
  "private": true,
  "type": "module",
  "description": "Aldus AI production runtime — monorepo root",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "npm run test --workspaces --if-present",
    "schema:generate": "npm run schema:generate -w @aldus-runtime/core",
    "schema:check": "npm run schema:check -w @aldus-runtime/core",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "npm run format:check && npm run build && npm run typecheck:test && npm run schema:check && npm run test",
    "typecheck:test": "npm run typecheck:test --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jamchen/aldus.git"
  },
  "homepage": "https://github.com/jamchen/aldus#readme",
  "bugs": {
    "url": "https://github.com/jamchen/aldus/issues"
  }
}
