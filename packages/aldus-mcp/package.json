{
  "name": "@aldus-runtime/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Aldus Production MCP — a capability-checked tool surface over @aldus-runtime/services (architecture contract §18.1).",
  "license": "UNLICENSED",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck:test": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@aldus-runtime/core": "*",
    "@aldus-runtime/services": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@aldus-runtime/file-store": "*",
    "@aldus-runtime/gate-engine": "*",
    "@aldus-runtime/stage-runner": "*",
    "@aldus-runtime/testkit": "*",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
